---
name: merger-restructuring-storyteller
description: 用于发现近期热门并购重组标的，并获取资料、撰写、修改或核查中文并购重组短视频分段脚本，适用于重大资产重组、资产置换、发行股份购买资产、换股吸收合并、控制权变化、表决权安排、同业竞争解决、挂牌公司重大资产交易等上市/挂牌公司交易视频。用户未指定标的时，先搜索过去七天的热门并购重组股票，逐只验证 ma_data_fetch.py 可取数后提供名称、代码和推荐理由供选择；用户已指定标的时直接进入资料流程。自动取数后依据首次公告日期检索巨潮资讯公告候选，必须等待用户确认主公告。始终以画面数据锁定交易步骤，并在分镜中至少展示一次用户确认的主公告截图；默认输出含来源核实的分段脚本、来源清单和纯文案版。
---

# 并购重组视频旁白

为已经做好的并购重组交易结构视频补旁白。视频本身已经展示交易步骤，任务不是重新设计交易结构，而是在不改变画面步骤的前提下，把交易为什么发生、主体之间是什么关系、每一步发生了什么讲清楚。

## 必要资料

- 画面数据文件，通常命名为 `标的名称原始数据.txt` 或 `.rtf`，里面包含交易前后状态和 `process` 交易步骤；可以由用户提供，也可以由内置工具生成。
- 公告、报告书、预案或说明书 PDF；可以由用户指定，也可以在自动检索后由用户确认标题再下载。

## 标的发现与选择门禁

用户没有明确指定公司、股票代码或本地交易资料时，先读 `references/workflow.md` 并执行近期标的发现：

1. 以上海时间当天为截止日，搜索最近 7 个自然日内披露并购重组首次公告、预案/报告书或重要进展的股票。
2. 只保留有实质交易事项的案例；排除传闻、股价异动、一般股权投资、股份质押、回购和没有具体交易安排的热点。
3. 以权威财经媒体报道数量、交易规模或结构影响、市场讨论度作为热度信号；每个候选至少提供一个可核实的近期事件和一个热度理由。
4. 收集至少 3 支候选后，运行 `tools/ma_data_fetch/ma_data_fetch.py verify-candidates 代码1 代码2 ... --json`。只有 `available: true` 的股票才能展示。
5. 向用户展示 3-6 个候选，每项包含标的名称、股票代码、近 7 日事件日期与标题、推荐理由。然后停止，等待用户选择。

不得用模型记忆生成“近期热门”名单，不得把未通过 `verify-candidates` 的股票放进选项。未指定标的时，禁止先询问用户股票代码、公司名称或让用户自行提供候选；标的发现与取数验证是本技能必须主动完成的第一项任务。若首轮可用候选不足 3 支，继续扩大搜索来源并验证同一 7 日窗口内的其他案例；仍不足时，展示全部已验证候选并说明数量不足的客观原因，不得退回为索要股票代码。若用户已明确指定标的，视为已经完成选择，跳过本门禁。

## 资料取得与公告确认门禁

先判断采用哪条资料路径，再进行步骤抽取和写稿：

### 用户提供模式

用户在当前请求或对话中明确附上、粘贴或指定画面数据与公告文件时，使用这两个输入。工作区、桌面、下载目录或历史上下文里存在同名文件，不等于用户已经指定；不得自行匹配或替用户选择本地文件。

### 自动获取模式

用户提供股票代码、明确要求自动取数，或缺少资料但允许联网时，读取 `references/workflow.md`，按以下顺序执行：

1. 运行 `tools/ma_data_fetch/ma_data_fetch.py fetch-data`，生成 `公司简称原始数据.txt`，记录公司简称、案例 ID 和首次公告日期。
2. 用公司简称、股票代码和首次公告日期运行 `find-announcements`，只获取并排序巨潮资讯公告候选。
3. 向用户展示候选公告的日期、完整标题和 PDF 链接，然后停止，明确请用户确认要使用的标题。
4. 用户确认具体标题后，运行 `download-announcement` 下载该 PDF。此时公告门禁才通过，才能抽取画面步骤、阅读公告和进入内容创作。

如果用户只给公司名而没有股票代码，先索要股票代码；如果用户明确禁止联网，则不能走自动获取模式，只索要缺失的本地资料。没有候选、候选冲突或标题未确认时，不得自动挑选公告，不得下载 PDF，不得写标题、步骤草案或旁白。

## 必读顺序

1. 判断用户是否已经指定标的；未指定时先完成最近 7 日热门标的发现、工具验证和用户选择。
2. 判断用户提供模式或自动获取模式，并向用户简要说明当前资料状态。
3. 读 `references/workflow.md`；自动获取模式先取画面数据、查公告候选，并在标题确认处暂停。
4. 两项资料都已指定或确认后，运行 `tools/transaction_data_inspector/transaction_data_inspector.py` 抽取交易步骤。
5. 用 `tools/pdf_reader/pdf_reader.py` 或当前环境的 PDF 工具读取已确认公告的关键章节，并记录适合截图的页码和核心条款。
6. 除非用户明确禁止联网，否则必须联网搜索补充资料。优先使用交易所公告、公司公告、公司官网和权威财经媒体。
7. 写初稿或润色前，读取 `references/writing_reference.md`。
8. 生成本地 Markdown 分段脚本时，运行 `tools/narration_lint/narration_lint.py` 做交付前检查。

## 硬性规则

- 用户未指定标的时，第一轮交付必须是 3-6 支已验证候选；禁止用“请提供股票代码”或同义追问代替候选发现。
- 资料取得与确认门禁优先于步骤分析和内容创作；允许为补齐输入执行取数和公告候选检索，但资料未确认齐全时不能写旁白、标题、小标题或交易步骤草案。
- 不能从工作区自动猜测用户想用的本地文件；即使目录里只有一个匹配文件，也必须先请用户确认。
- 自动获取模式生成的原始数据可以作为画面数据输入；公告必须由用户确认完整标题，不能根据排名、日期或关键词自动选定。
- 公告候选确认前只展示候选，不下载 PDF；用户确认后才运行下载命令并进入创作。
- 最终分镜必须至少一次使用 `【画面】：公告截图`，且只能截图用户在公告门禁中确认的主公告；不得用后续自行搜索的公告替代。
- `公告截图` 段落的 `【核实内容】` 必须写明公告完整标题、页码和建议截取的核心文字，旁白事实必须与该页内容一致。
- `公告截图` 只是段落的 `【画面】` 类型，不是段落标题，更不能单独写成 `## 公告截图`、`### 公告截图` 或任何同义小标题。开头/结尾小标题和画面步骤小标题都必须保持叙事功能或沿用画面步骤名，不能用画面类型充当标题。
- 画面数据是交易步骤数量、名称和顺序的第一依据。
- 联网资料和公告不能替代画面数据来确定步骤数量、顺序和小标题。
- 不得因为公告里出现更多法律安排，就把一个画面步骤拆成多个旁白步骤。
- 不得新增画面数据里没有的交易步骤。
- 公告只用于补充交易背景、交易目的、主体关系、关键数字和必要概念解释。
- 如果公告、预案只给出 `数字经济`、`人工智能`、`新质生产力`、`产业升级` 等宏观表述，不能直接搬进旁白当作原因。必须继续搜索公司公开表达、交易所问答、董秘回复、公司公众号、官媒、权威财经媒体等资料，补足“上市公司为什么需要这笔交易、标的为什么合适、交易后解决什么问题、仍有什么风险”的具体解释。
- 解释跨行业并购时，优先建立四段因果链：原主业压力或瓶颈；标的公司的具体业务和业绩支撑；交易后业务结构怎么变；跨界整合风险或边界。宏观行业趋势只能作为辅助背景，篇幅不得盖过交易过程。
- 公告与画面数据不一致时，按画面数据组织步骤；只有差异会影响观众理解时，才在核对说明里简要说明。
- 关键主体第一次出现时，必须自然说明它是谁、和上市/挂牌公司、标的公司、交易对方、股东、投资方、控股股东或实控人是什么关系。
- 涉及控制权、表决权、同业竞争、资产置换、吸收合并、业务调整等概念时，只在理解交易需要时用通俗语言解释。
- 只保留推动理解的关键数字。
- 开头第一段必须先提出观众能立刻理解的核心问题或矛盾，例如“做 A 的公司，为什么突然买 B？”、“小平台为什么要装入大资产？”、“这笔交易到底解决什么压力？”。问题要来自交易事实，不能夸张、煽动或标题党。
- 提问之后必须迅速给出必要主体和背景，说明观众接下来要看什么，避免一上来只交代公司名、公告名或业务定义。
- 写初稿和润色检查时都必须限制 `不是...而是...` 这类虚假对立句式。主版正文中 `不是` 和 `而是` 各自最多出现 1 次，优先清零；出现时必须确实承担事实辨析功能。
- 写初稿和润色检查时要清理绝对化表达，尤其避免 `最值得看`、`最核心`、`最关键`、`真正改变的是`、`真正要看的是`、`真正的...是`。优先改成 `要看的是`、`这一步改变的是`、`需要关注的是`、`交易要回答的问题是` 等克制表达。
- 正文不得出现 `画面数据里`、`画面显示`、`视频里`、`对应画面` 这类制作视角或抽象素材视角。口播要直接讲交易动作；来源核实内容可以说明依据来自画面数据。
- 正文不得使用 `公司解释`、`资料显示` 这类模糊来源表达。需要归因时，直接点名 `业绩预告中提到`、`停牌公告披露`、`年报显示`、`证券时报报道` 等具体来源类型或来源名称。
- 用词必须通俗自然，不得使用生硬、别扭或不符合中文搭配的表达，例如 `跨进存储器`。应改为 `收购一家存储器公司`、`进入存储器领域`、`新增存储器业务` 等自然说法。
- 涉及多名交易对方、股东、投资者或节点时，正文通常只点名前两个代表主体，再写 `等 N 名交易对方` 或 `等主体`；完整名单放入来源核实内容或资料汇总，避免口播啰嗦。
- 不写投资建议、股价暗示、夸张判断或没有来源支撑的确定性表达。
- 不机械复述审批流程、停复牌、风险提示、尚未签约、不确定性等流程性内容，除非这些信息对理解交易非常必要。
- 旁白必须服务既有画面顺序。它不是法律备忘录，也不是公告全文摘要。

## 默认交付

除非用户要求文件包或核查版，默认直接在对话中输出：

1. `视频大标题`：放在文案最前面，客观严谨，像严肃新闻标题。
2. `分段脚本（含来源核实）`：每段文案后紧跟 `【画面】`、`【来源】`、`【核实内容】`；`html chart` 段落必须在 `【画面】` 后额外紧跟 `【画面数据】`；画面步骤小标题必须作为脚本内部的段落标题出现，沿用画面数据中的步骤名。
3. `来源`：紧跟含来源核实版，列出编号、标题、链接或路径及核实用途。
4. `纯文案版`：固定放在来源之后，重新写出相同的 `视频大标题`，保留分段脚本中的全部段落小标题和正文，删除 `【画面】`、`【画面数据】`、`【来源】`、`【核实内容】`。

默认不得在文末单独列 `小标题：步骤1 / 步骤2 / ...`，因为这样无法判断小标题应放在视频哪里。默认也不得输出 `快节奏版`、`短版`、`配音提示`、分镜脚本或其他附加栏目。纯文案版是默认交付的一部分，不得因用户未单独提出而省略；用户明确要求只要其中一个版本时，才按其要求删减。

### 画面类型五选一

只能使用以下五种画面类型，不得把多个类型拼成 `新闻+公告截图`、`公告截图+b-roll素材` 等复合标签：

| 画面类型 | 使用场景 |
|---|---|
| `并购重组范式` | 涉及并购重组具体步骤、交易结构、股份/资产/现金/表决权移动、交易前后状态变化；文案依据主要来自用户指定或自动获取的画面原始结构数据。 |
| `公告截图` | 展示用户在公告确认门禁中选定的主公告。优先截取首页标题、交易概述或与当前旁白直接对应的核心条款；每篇脚本至少使用一次。 |
| `html chart` | 数据完整且可直接形成柱状图、折线图、对比图或比例图的量化段落。原因解释、业务介绍、主体关系和叙事提问不使用此类型。 |
| `白板` | 涉及概念拆解、原因解释、现象解释、制度机制、风险边界或“为什么这样安排”的推理说明。 |
| `b-roll素材` | 空镜头或真实视频/图片素材。文案不适用前三类时使用，包括公司业务、产品、工厂、城市、办公、发布会、新闻环境、过渡和收束。 |

`html chart` 必须满足以下条件：

- 将叙事句和数据句拆段；叙事句使用 `白板` 或 `b-roll素材`，只有量化数据句使用 `html chart`。
- 在 `【画面】：html chart` 后立即输出 `【画面数据】`，优先用 Markdown 表格列明 `主体`、`指标`、`期间`、`数值`、`单位`。
- 多主体对比必须给出每个主体的分项数据。只有合计数时，先通过公告、年报、工具或权威公开资料补齐；补不齐则改用 `白板` 或 `b-roll素材`，不得硬做图表。
- `【核实内容】` 说明数据出处及计算口径；计算值必须同时保留原始分项和计算方法。

如果用户要求文件交付，建立项目目录，默认只输出：

- `标的名称并购重组文案.md`：合并包含视频大标题、分段脚本（含来源核实）、来源和纯文案版。
- 文案实际引用的公告 PDF：复制或下载到同一目录，Markdown 来源路径与文件名保持一致。

只有用户明确要求完整核查包时，才额外输出：

- `交易步骤核对.md`
- `资料汇总.md`
- `AI模拟核稿.md`

具体模板见 `references/workflow.md`。

## 资源索引

| 资源 | 用途 |
|---|---|
| `references/workflow.md` | 完整工作流、步骤抽取表、资料策略、写作顺序、文件模板和交付检查 |
| `references/writing_reference.md` | 中文短视频旁白风格、概念转译、禁用表达和标题规则 |
| `tools/ma_data_fetch/ma_data_fetch.py` | 按股票代码获取画面原始数据、依据首次公告日期查找巨潮资讯候选，并在标题确认后下载公告 PDF |
| `tools/transaction_data_inspector/transaction_data_inspector.py` | 解析画面数据，输出按顺序排列的交易步骤核对表 |
| `tools/pdf_reader/pdf_reader.py` | 按页读取公告 PDF |
| `tools/news_search/news_search.py` | 可用时查询财联社近期财经资讯 |
| `tools/report_search/report_search.py` | 配置 `AICUBES_AUTHORIZATION` 时查询研报资料 |
| `tools/narration_lint/narration_lint.py` | 检查最终 Markdown 是否包含必要栏目、来源核实格式和不宜出现的解释腔 |

## 交付前检查

- 已在写稿前抽取画面数据中的交易步骤数量和顺序。
- 画面步骤小标题与画面数据中的步骤名一致；开头和结尾小标题必须有叙事功能，避免模板占位标题。
- 关键主体在交易动作前已经交代清楚。
- 开头先提出核心问题或矛盾，并让观众知道接下来要看什么；不能只复述公告名称、公司业务或日期。
- 已把宏观话术翻译成具体因果：公司为什么要做、为什么选这个标的、交易后状态怎么变；没有用行业大词代替解释。
- 每个步骤只讲本步骤发生了什么、谁给谁、交易标的是什么、结果是什么。
- 没有新增交易步骤、重复解释或堆砌流程性信息。
- 必要概念已经用通俗语言解释。
- 已检查 `不是`、`而是` 句式数量，主版正文中各自不超过 1 次，且没有无意义的对立表达。
- 已检查绝对化表达，正文没有 `最值得看`、`最核心`、`真正改变的是`、`真正要看的是` 这类过满句式。
- 已检查正文没有 `画面数据里`、`画面显示`、`公司解释`、`资料显示` 等抽象或模糊表达；交易动作直接讲，来源归因具体点名。
- 已检查是否存在生硬搭配、奇怪动词或不自然口语；多名主体是否压缩为前两个代表主体加 `等 N 名`。
- 已联网补充资料，或已说明未联网的限制。
- 用户未指定标的时，已完成最近 7 日热门案例搜索；每个展示候选均有近期事件、热度理由并通过 `verify-candidates` 实际取数验证。
- 最终输出包含文案开头的 `视频大标题` 和嵌入步骤小标题的 `分段脚本（含来源核实）`；每段都有 `【画面】`、`【来源】`、`【核实内容】`，不得把小标题单独列在文末。
- 主版至少有一段 `【画面】：公告截图`，使用的是用户最初确认的主公告；核实内容包含公告完整标题、页码和建议截取文字。
- 已检查没有把任何画面类型写成段落标题，尤其不能出现 `## 公告截图` 这类把画面类型误当小标题的写法。
- 所有 `html chart` 段落都只包含可直接制图的量化文案，并在 `【画面】` 后紧跟完整的 `【画面数据】`；多主体对比没有遗漏分项。
- 来源清单之后已追加 `纯文案版`；纯文案版重新包含相同的视频大标题，完整保留全部段落小标题和正文，并删除画面、画面数据、来源、核实内容。
- 文件交付只生成一个合并 Markdown，并附上正文实际引用的公告 PDF；除非用户明确要求完整核查包。

## User-Learned Best Practices & Constraints

> **Auto-Generated Section**: This section is maintained by `skill-evolution-manager`. Do not edit manually.

### User Preferences
- 并购重组短视频开头不能只介绍公司或公告，必须先提出观众能理解的核心问题或交易矛盾，让人知道接下来要看什么；提问要克制、来自事实，不能标题党。
- 用户需要文件交付时，默认可以同时输出含来源核实的分段脚本 Markdown 和同结构纯文案 Markdown，纯文案版删除画面、来源、核实内容，方便配音。
- 并购重组口播正文不要用‘最……’、‘真正的……是’、‘真正改变的是’、‘真正要看的是’这类非常绝对的表达，避免把分析语气说满。
- 并购重组口播正文不要用‘画面数据里’、‘画面显示’、‘视频里’这类制作视角或素材视角表达，正文应直接讲交易动作。
- 并购重组口播正文不要用‘公司解释’、‘资料显示’这类模糊归因；需要归因时直接点名来源类型或来源名称，如‘业绩预告中提到’、‘停牌公告披露’、‘年报显示’、‘证券时报报道’。
- writing_reference.md 必须保留三个完整参考 case，不能迭代时退化或覆盖既有 case。
- 并购重组默认交付必须同时包含含来源核实的分段脚本和纯文案版；纯文案版放在来源清单之后，并保留与主版一致的视频大标题和全部段落小标题。
- html chart 只用于数据完整、可直接形成柱状图、折线图、对比图或比例图的量化段落；原因解释、业务介绍、主体关系和叙事提问应拆段并改用白板或 b-roll素材。
- 所有 html chart 段落必须在【画面】后紧跟【画面数据】，结构化列明主体、指标、期间、数值和单位；纯文案版删除【画面数据】。
- 用户要求文件输出或交付包时，默认只生成一个合并 Markdown，内部依次包含视频大标题、含来源核实的分段脚本、来源清单和纯文案版；不要再拆成多个核对或文案 Markdown。
- 文件交付除合并 Markdown 外，还要一并输出该文案实际引用的公告 PDF；来源清单中的本地公告路径必须指向交付目录内对应文件。
- 三个完整参考 case 分别为海联讯、恒尚节能、思存科技；新增 case 时不得覆盖或压缩既有 case。
- 三个参考 case 中所有 html chart 段落都必须在【画面】后紧跟结构化【画面数据】表格。
- 并购重组短视频面向普通观众时，开头优先从生活经验或观众利益切入，可用自然互动提问建立代入感；第一句避免直接出现收购、产业基金、资产注入、同业竞争等专业词，先讲人话，再逐步引入正式名称。
- 开头提出的问题必须把真实矛盾说清楚，并解释这件事为什么与观众有关；不能只制造疑问。适合的矛盾包括共同出资却没有控制权、相近业务分处上市公司内外、资产留在体外还是进入上市公司。
- 关键主体第一次出现时必须同时说明它是什么、做什么、由谁控制以及与上市公司的关系；不得使用‘它背后是间接控股股东’等未点名、无前置指代的悬空表达。
- 背景部分只服务于回答交易为什么发生、为什么现在发生和要解决什么问题；减资、退伙顺序等程序细节若不推动理解且不属于画面交易步骤，应删除或后置。
- 每段背景信息都要说明其叙事作用。提到关联交易时，应通俗说明交易双方存在关联以及这对审议和信息披露意味着什么；背景结尾应明确交易原因，并用自然问题或结果句衔接画面交易步骤。
- 画面类型按段落功能判断：只有直接描述资产、股权、现金、股份或控制权移动的画面交易步骤使用‘并购重组范式’；概念、关系、原因、机制和路径解释使用‘白板’；公司、工厂、业务、资产、历史承诺、风险进展等非解释性事实叙述使用‘b-roll素材’，并注意白板与b-roll整体均衡。
- writing_reference.md 的完整参考案例现为四个：海联讯、恒尚节能、思存科技和方大特钢；后续迭代不得覆盖、压缩或删除任何既有案例。
- 方大特钢 case 4 作为产业基金先行收购、基金退出时以实物分配等形式注入上市公司、解决潜在同业竞争的标准案例；必须保留生活化互动开头、陌生主体逐层介绍、背景到单一步骤的自然过渡、实物分配解释边界、一季度 html chart 和画面类型分工。
- 用户未指定标的时，skill 的第一项任务必须搜索上海时间最近7个自然日内热门且有实质并购重组事件的多支股票；每个候选包含名称、代码、近7日事件和选择理由，并在展示前通过 ma_data_fetch.py 实际取数验证。
- 分镜新增独立画面类型‘公告截图’；默认主版至少出现一次，只能展示用户在公告确认门禁中选定的主公告，核实内容写明完整标题、页码和截图重点。
- 用户未指定并购重组标的时，第一轮必须直接输出过去7个自然日内的多支热门候选，不得先询问股票代码、公司名称或要求用户自行提供候选。

### Known Fixes & Workarounds
- 将旧的主版旁白输出格式升级为分段脚本（含来源核实），每段紧跟画面类型、来源和核实内容，并保留来源清单。
- 开头小标题避免模板化的开场/收束，占位标题应改成跨界悬念、主业承压、平台错配、整合考验等有叙事功能的小标题。
- 将绝对化强调改成更克制的事实表达，例如‘要看的是’、‘需要关注的是’、‘这一步改变的是’、‘交易要回答的问题是’。
- 把‘画面数据里，这次交易集中在一个步骤’改成‘这次交易集中在一个步骤’，把‘公司解释’改成‘业绩预告中提到’。
- 保留 writing_reference.md 中海联讯、恒尚节能和思存科技三个完整 case，并在完整参考基础上叠加新规则。
- 将纯文案版从按需附加项改为默认必选项；对话交付顺序固定为视频大标题、分段脚本（含来源核实）、来源、纯文案版。
- 遇到拟做主体对比图但只有合计数时，先通过公告、年报、工具或权威公开资料补齐各主体分项；数据补不齐则不得输出 html chart，应改用白板或 b-roll素材，并避免用不完整比较误导画面制作。
- 混合了叙事解释和完整数据的段落必须拆开：叙事句单独使用白板或 b-roll素材，数据句单独使用 html chart 并附【画面数据】。
- 文件交付目录改为一个合并文案 Markdown 加实际使用的公告 PDF；只有用户明确要求完整核查包时，才额外生成交易步骤核对、资料汇总和 AI 模拟核稿等文件。
- 将思存科技收购兴豪通信51.44%股权、宁波矽久100%股权并募集配套资金的成稿沉淀为 case 3，示范同产业链补链、连续亏损背景、重大重组比例和预案阶段风险边界的写法。
- 开头信息过载时，改用‘生活类比或互动提问—具体公司遇到同类问题—逐步介绍平台和正式主体—点明交易矛盾’的顺序，避免第一段同时堆入产业基金、先行收购、资产注入、钒钛等多层概念。
- 主体段落出现陌生名称时，先用上一段已经出现的通俗角色承接，例如‘前面说的这个平台，名字叫南昌沪旭’，再介绍主要出资方、管理方及控制关系。
- 将‘基金退出分成前后衔接安排’等不解释交易原因、又不属于画面步骤的程序段落从背景删除，防止背景尚未收束就进入流程细节。
- 解释‘实物分配’等陌生概念时，先说明观众容易误解的含义，再结合公告目标给出通俗解释；若具体方案尚未披露，必须在核实内容中标明解释边界，不能把推断写成确定事实。
- 用户提供改写稿后，优先保留其叙事语言和段落结构，逐句核验数字、主体关系、承诺期限和交易状态；完整量化数据单独拆成html chart，推断性表述改成有边界的解释。
- 交易步骤前增加承上启下句：背景先落到要解决的矛盾或历史承诺，再用‘接下来的问题是这些企业怎样转到上市公司手里’之类的自然过渡引出画面步骤。
- 将方大特钢筹划取得钢铁企投控制权的完整核实稿沉淀为 case 4，示范共同出资不等于取得控制权、产业基金体外收购后注入、关联方关系解释、36个月资产注入承诺及筹划阶段风险边界的写法。
- 方大特钢 case 4 中仅‘退出南昌沪旭并取得钢铁企投控制权’及‘通过实物分配承接控制权’两段使用并购重组范式；主体和机制解释用白板，资产、承诺和风险事实用b-roll，完整财务数据用html chart。
- ma_data_fetch.py 新增 verify-candidates 批量验证命令，使用临时目录实际获取同花顺案例数据，并输出 available、公司简称、案例ID和首次公告日期；不可获取的股票不得进入热门候选。
- 画面类型由四选一升级为五选一：并购重组范式、公告截图、html chart、白板、b-roll素材；禁止‘公告截图+b-roll素材’等复合标签，narration_lint.py 同步校验独立公告截图。
- 热门标的发现只在用户未明确指定公司、股票代码或本地资料时触发；用户点名标的视为已经完成选择，直接进入取数和公告确认流程。
- 若近7日候选经 ma_data_fetch.py 验证后不足3支，应继续扩大权威信息源寻找同一时间窗口内的案例；仍不足时展示全部已验证候选并说明原因，不能退回为索要股票代码。

### Custom Instruction Injection

更新完整参考样稿时，逐个检查所有 case 的 html chart 段落，确保【画面数据】字段紧随【画面】且包含主体、指标、期间、数值、单位。
